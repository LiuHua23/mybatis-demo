<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwa.mybatis.dao.StudentDao">
    
    <insert id="insertStudent" parameterType="Student" keyProperty="id" useGeneratedKeys="true">
        insert into students(name,age,gender,number)
        values(#{name},#{age},#{gender,typeHandler=org.apache.ibatis.type.EnumTypeHandler},#{number})
    </insert>
    
    <delete id="deleteStudentByNumber">
        delete from students where number=#{number}
    </delete>

    <update id="updateStudentByNumber" parameterType="Student">
        update sutdents set name=#{name},age=#{age},
        gender=#{gender}
        where number=#{number}
    </update>
    
    <select id="selectStudentByNumber" resultType="Student">
        select * from students where number=#{number}
    </select>
</mapper>